export const selectAllSpaces = (reduxState) =>
  reduxState.giveTheFirstName.change;

export const loadingSpaces = (reduxState) =>
  reduxState.giveTheFirstName.loading;

export const spaceDetailsShowStories = (reduxState) =>
  reduxState.giveTheFirstName.change.its_The_Same_name_1;

export const selectUserSpace = (reduxState) => reduxState.user.space;

export const selectJustTheUser = (reduxState) => reduxState.user;
